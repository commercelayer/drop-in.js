import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { meta, Basic, Message } from './cl-availability-info.stories.ts';

<Meta
  title={meta.title}
  argTypes={meta.argTypes}
/>

# `cl-availability-info`

As you saw in the examples above, `cl-availability-info` is a placeholder used to display the availability message.

<Canvas withSource="open">
  <Story story={Basic} name="Basic" />
</Canvas>

<ArgsTable story="Basic" />

### Message

You can combine different `cl-availability-info` types to compose the message that fits your needs.

<Canvas withSource="open">
  <Story story={Message} name="Message" />
</Canvas>
