import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { meta, Basic } from './cl-availability-message.stories.ts';

<Meta
  title={meta.title}
  argTypes={meta.argTypes}
/>

# `cl-availability-message`

As you saw in the examples above, `cl-availability-message` is a placeholder used to display the availability message.

<Canvas withSource="open">
  <Story story={Basic} name="Basic" />
</Canvas>

<ArgsTable story="Basic" />
