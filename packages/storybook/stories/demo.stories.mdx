import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { meta, Basic } from './demo.stories.ts';

<Meta
  title={meta.title}
  argTypes={meta.argTypes}
/>

# `Demo` 🚀

This is a demo of most of our `drop-in.js` components. We are using Tailwind to speed up the layout building.

We called it demo, but at the end by switching to [live mode](https://docs.commercelayer.io/core/api-specification#environments) this can be considered a production website 😮.

<Canvas withSource="close">
  <Story story={Basic} name="Basic" />
</Canvas>

<ArgsTable story="Basic" />
