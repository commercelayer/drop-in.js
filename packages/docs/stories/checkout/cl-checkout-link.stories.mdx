import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { meta, Basic } from './cl-checkout-link.stories.ts';
import { codes } from '../assets/constants.ts'

<Meta
  title={meta.title}
  argTypes={meta.argTypes}
/>

# Checkout

Any Commerce Layer account comes with a hosted version of the [open-source Checkout application](https://github.com/commercelayer/commercelayer-react-checkout) that is automatically enabled. It's a React-based, PCI & PSD2-compliant, production-ready implementation that developers can integrate with just a single URL link as explained in detail [here](https://github.com/commercelayer/commercelayer-react-checkout#hosted-version).

Once you have [imported](?path=/docs/getting-started--page) the drop-in library, you can use the `cl-checkout-link` component as an in-place replacement for an `<a>` tag pointing to that URL with the option to specify the `target`, as shown in the code snippet below.

<Canvas withSource="open">
  <Story story={Basic} name="Basic" />
</Canvas>

<ArgsTable story='Basic' />

### Empty cart

The checkout link rendered by the component is disabled when the cart is empty (or does not contain any shoppable item).
As soon as you <cl-add-to-cart class="mx-1 px-2 py-1" code={codes.available}>add to cart</cl-add-to-cart> a product, the link becomes clickable (feel free to try it out by emptying your <cl-cart-link class="font-bold text-[#1EA7FD]">cart<cl-cart></cl-cart></cl-cart-link> first and then clicking on the in-line button — which by the way was *dropped in* this sentence using the [**cl-add-to-cart**](?path=/docs/components-add-to-cart-cl-add-to-cart--basic) component). 😉